/*
* These are tests to ensure the engine is working correctly
*/

:start
inv- 1,Gold
inv+ 1,Gold
btn proc test_proc, Test PROC
btn proc test_input, Test INPUT
end

/*
* PROC
*/

:test_proc
pln Next are the numbers from 1 to 3 in order:
proc 123
end

:123
proc 1
end

:1
pln 1
proc 2
end

:2
pln 2
proc 3
end

:3
pln 3
end

/*
* INPUT
*/

:common
end

:test_input
pln Enter something:
input x
pln This is what you entered: #x$
end

:Use_Gold_Examine.
pln Gold coins that can be used to buy everything, everywhere.
end

:Use_Gold_Throw.
pln How much gold to throw away?
input namegold
if namegold>0 then pln You threw away the gold.
if namegold>0 then inv- #namegold$,Gold
if namegold<0 then pln What a crook you are!!! No, that won't work.
end
